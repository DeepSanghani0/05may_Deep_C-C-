//this is exercise
//find area of circle(radius is given)

#include<stdio.h>
int main() {
    float radius;
    float pi = 3.14;
    printf("enter the vlaue radius ");
    scanf("%f",&radius);

printf("area of circle is %f",pi * radius * radius);
    return 0;
}