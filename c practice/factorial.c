#include<stdio.h>
#include<math.h>
int factorial(int n);
int main(){
    int n;
    printf("enter the number");
    scanf("%d",&n);
    printf("factorial of number is %d", factorial(n));
    return 0;
}

int factorial(int n)
{
    return 0;
}
